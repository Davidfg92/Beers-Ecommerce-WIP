/* eslint-disable react/react-in-jsx-scope */
import { render, screen } from '@testing-library/react';

import Registerpage from './Registerpage';

describe('When registerpage called', () => {
  test('Then print registerpage', () => {
    render(
      <Registerpage />,
    );

    expect();
  });
});
