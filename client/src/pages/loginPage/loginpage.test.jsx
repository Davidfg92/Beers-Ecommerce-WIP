/* eslint-disable react/react-in-jsx-scope */
import { render, screen } from '@testing-library/react';

import Loginpage from './Loginpage';

describe('When loginpage called', () => {
  test('Then print loginpage', () => {
    render(
      <Loginpage />,
    );

    expect();
  });
});
