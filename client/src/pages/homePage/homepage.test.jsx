/* eslint-disable react/react-in-jsx-scope */
import { render, screen } from '@testing-library/react';

import Homepage from './Homepage';

describe('When homepage called', () => {
  test('Then print homepage', () => {
    render(
      <Homepage />,
    );

    expect();
  });
});
