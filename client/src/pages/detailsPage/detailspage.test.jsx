/* eslint-disable react/react-in-jsx-scope */
import { render, screen } from '@testing-library/react';
import Detailspage from './Detailspage';

describe('When detailspage called', () => {
  test('Then print detailspage', () => {
    render(
      <Detailspage />,
    );

    expect();
  });
});
