/* eslint-disable react/react-in-jsx-scope */
import { render, screen } from '@testing-library/react';
import Cartpage from './Cartpage';

describe('When cartpage called', () => {
  test('Then print cartpage', () => {
    render(
      <Cartpage />,
    );

    expect();
  });
});
